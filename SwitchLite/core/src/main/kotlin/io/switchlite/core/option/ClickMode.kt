package io.switchlite.core.option

/**
 * Click mode for AutoClicker.
 * SINGLE: one click per interval.
 * DOUBLE: two clicks per interval (jitter/double click).
 */
enum class ClickMode { SINGLE, DOUBLE }
